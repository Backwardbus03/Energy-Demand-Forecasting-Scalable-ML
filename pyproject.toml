[project]
name = "energy-forecasting-mlops"
version = "0.1.0"
description = "Regional electricity demand forecasting - MLOps pipeline on EIA API v2"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py310"
